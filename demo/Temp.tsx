import React from 'react';

// This file is intentionally left minimal to resolve build errors
// related to missing dependencies in the '/temp' directory.
export const TempDemo = () => {
    return null;
};
